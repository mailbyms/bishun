# 笔顺 - 汉字笔顺学习应用

一款业余的汉字笔顺学习和练习应用，通过交互式动画帮助用户掌握正确的汉字书写顺序。

## ✨ 主要特性

### 🎯 核心功能
- **动态笔顺演示** - 逐笔展示汉字正确书写顺序

### 项目结构
```
app/
├── src/main/
│   ├── java/com/gyjian/bishun/
│   │   ├── ui/           # UI组件和界面
│   │   ├── data/         # 数据层和网络请求
│   │   ├── domain/       # 业务逻辑
│   │   ├── utils/        # 工具类
│   │   └── MainActivity.kt
│   ├── res/
│   │   ├── drawable/     # 图片资源（包括logo）
│   │   ├── layout/       # 布局文件
│   │   ├── values/       # 字符串资源
│   │   └── assets/       # 静态资源（SVG动画）
│   └── assets/
│       └── stroke_svgs/  # 汉字笔顺SVG文件
└── build.gradle.kts      # 构建配置
```

## 📦 APK构建

### 构建命令
```bash
# 调试版本
./gradlew assembleDebug

# 发布版本
./gradlew assembleRelease

# 清理项目
./gradlew clean

# 运行测试
./gradlew test
```

### APK命名规则
应用采用智能命名规则：`{应用名称}_v{版本号}_{构建类型}_{架构}.apk`

示例：
- `bishun_v1.0_release_arm64-v8a.apk`
- `bishun_v1.0_debug_universal.apk`

## 🔧 开发指南
- 汉字SVG文件来自 https://github.com/skishore/makemeahanzi
- SVG文件命名规则：x.charCodeAt(0) 再加后缀 .svg
- 本项目修改了 svg 文件里的颜色，统一为深红色 crimson
  ```zsh
  sed -i '' -E 's/\b(black|blue)\b/crimson/g' *.svg(N)
  ```



